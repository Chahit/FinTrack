'use client';

import { Asset } from '@/lib/types';

export interface PriceAlertsProps {
  assets: Asset[];
}

export function PriceAlerts({ assets }: PriceAlertsProps) {
  return (
    <div>
      {/* Price alerts implementation */}
      Price Alerts Component
    </div>
  );
} 